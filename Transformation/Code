# Ingestion queries:
SELECT 
	cst_id, count(*)
FROM 
	ingestion.crm_cust_info
GROUP BY 
	cst_id
HAVING 
	count(*) > 1 or cst_id is null


SELECT * FROM 
	ingestion.crm_cust_info 
WHERE 
	cst_id is NULL

SELECT * FROM 
	ingestion.crm_cust_info 
WHERE
	cst_key != CONCAT('AW000', cst_id)

SELECT * FROM 
	ingestion.crm_cust_info 
WHERE 
	cst_firstname != TRIM(cst_firstname)



# After transformation queries: (no more duplicates and null, free spaces removed)
SELECT 
	cst_id, count(*)
FROM 
	transformation.crm_cust_info
GROUP BY 
	cst_id
HAVING 
	count(*) > 1 or cst_id is null


SELECT * FROM 
	transformation.crm_cust_info 
WHERE 
	cst_id is NULL

SELECT * FROM 
	transformation.crm_cust_info 
WHERE
	cst_key != CONCAT('AW000', cst_id)

SELECT * FROM 
	transformation.crm_cust_info 
WHERE 
	cst_firstname != TRIM(cst_firstname)


ALTER TABLE ingestion.crm_prd_info
ADD cat_id VARCHAR(5);

ALTER TABLE transformation.crm_prd_info
ADD cat_id VARCHAR(5);

SELECT * FROM ingestion.crm_prd_info;

SELECT * FROM transformation.crm_prd_info;

SELECT * FROM transformation.crm_sales_details
WHERE sls_price IS NULL;

SELECT sls_quantity, count(*)
FROM transformation.crm_sales_details
WHERE sls_quantity > 1
GROUP BY sls_quantity



